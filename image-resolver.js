(() => {
  // Disabled: resolving every Pokémon card through a live API caused hundreds
  // of requests and kept the page in a permanent loading state.
  // Card images now load directly from the static URLs in data.js.
})();
